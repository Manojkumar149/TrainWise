# Backend - Smart SOP & Training Assistant

## Setup

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Create a `.env` file based on `.env.example` to configure the database.
